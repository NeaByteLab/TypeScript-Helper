{
  "code": 1010,
  "message": "'*/' expected.",
  "regexFind": "/\\/\\*([^*]|\\*(?!\\/))*$/",
  "regexReplace": "$& */",
  "suggestion": "Add closing comment delimiter '*/' to terminate the block comment",
  "example": [
    {
      "base": "/* console.log('This is commented out')",
      "fixed": "/* console.log('This is commented out') */",
      "description": "Block comments must be properly closed with '*/'"
    }
  ]
}