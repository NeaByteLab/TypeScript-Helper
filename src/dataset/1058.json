{
  "code": 1058,
  "message": "The return type of an async function must either be a valid promise or must not contain a callable 'then' member.",
  "regexFind": "/async\\s+function\\s+([a-zA-Z_$][a-zA-Z0-9_$]*)\\s*\\(([^)]*)\\)\\s*:\\s*([A-Z][a-zA-Z0-9_$]*)/",
  "regexReplace": "async function $1($2): Promise<$3>",
  "suggestion": "Change return type to Promise<T> or remove the 'then' method. Async functions must return a valid Promise or a type without callable 'then' method.",
  "example": [
    {
      "base": "async function testAsync(): NotAPromise {",
      "fixed": "async function testAsync(): Promise<NotAPromise> {",
      "description": "Change return type to Promise for async function"
    },
    {
      "base": "async function getData(): CustomPromise {",
      "fixed": "async function getData(): Promise<CustomPromise> {",
      "description": "Use Promise return type for async function"
    }
  ]
}
