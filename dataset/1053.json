{
  "code": 1053,
  "message": "A 'set' accessor cannot have rest parameter.",
  "regexFind": "/set\\s+([a-zA-Z_$][a-zA-Z0-9_$.-]*)\\s*\\(\\s*\\.\\.\\.([^)]+)\\)/",
  "regexReplace": "set $1($2)",
  "suggestion": "Replace rest parameter with a single parameter. Set accessors cannot have rest parameters - they must have exactly one parameter.",
  "example": [
    {
      "base": "set value(...args: string[]) {",
      "fixed": "set value(args: string[]) {",
      "description": "Replace rest parameter with single parameter in set accessor"
    },
    {
      "base": "set data(...items: any[]) {",
      "fixed": "set data(items: any[]) {",
      "description": "Replace rest parameter with single parameter in set accessor"
    }
  ]
}