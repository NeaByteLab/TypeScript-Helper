{
  "code": 1035,
  "message": "Only ambient modules can use quoted names.",
  "regexFind": "/module\\s+\"([^\"]+)\"/",
  "regexReplace": "declare module \"$1\"",
  "suggestion": "Add declare keyword to make it an ambient module. Only ambient modules (declare module) can use quoted names for module declarations.",
  "example": [
    {
      "base": "module \"my-module\" {\n  export const value = 42\n}",
      "fixed": "declare module \"my-module\" {\n  export const value = 42\n}",
      "description": "Add declare keyword to make it an ambient module"
    },
    {
      "base": "module \"@types/node\" {\n  export interface Process {\n    env: any\n  }\n}",
      "fixed": "declare module \"@types/node\" {\n  export interface Process {\n    env: any\n  }\n}",
      "description": "Convert to ambient module declaration with declare keyword"
    }
  ]
}