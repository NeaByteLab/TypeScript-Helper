{
  "code": 18047,
  "message": "'{0}' is possibly 'null'.",
  "regexFind": "/([a-zA-Z_$][a-zA-Z0-9_$]*)\\.(length|toUpperCase|trim|\\w+)/g",
  "regexReplace": "$1?.$2",
  "suggestion": "Add null check or use optional chaining to handle the possibility of null values. Use nullish coalescing operator (??) or optional chaining (?.) for safe property access.",
  "example": [
    {
      "base": "function processValue(value: string | null) {\n  console.log(value.length)\n  const upper = value.toUpperCase()\n  return value.trim()\n}",
      "fixed": "function processValue(value: string | null) {\n  if (value === null) {\n    return\n  }\n  console.log(value.length)\n  const upper = value.toUpperCase()\n  return value.trim()\n}",
      "description": "Add null check before accessing properties"
    },
    {
      "base": "const result = data.trim()",
      "fixed": "const result = data?.trim() ?? ''",
      "description": "Use optional chaining and nullish coalescing"
    }
  ]
}