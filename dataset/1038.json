{
  "code": 1038,
  "message": "A 'declare' modifier cannot be used in an already ambient context.",
  "regexFind": "/\\s+declare\\s+/",
  "regexReplace": " ",
  "suggestion": "Remove declare modifier from ambient context. The declare keyword is redundant inside already ambient contexts like declare module or declare namespace.",
  "example": [
    {
      "base": "declare module \"my-module\" {\n  declare const value = 42\n}",
      "fixed": "declare module \"my-module\" {\n  const value: number\n}",
      "description": "Remove declare modifier and add type annotation instead of initializer"
    },
    {
      "base": "declare namespace MyNamespace {\n  declare interface Config {\n    name: string\n  }\n}",
      "fixed": "declare namespace MyNamespace {\n  interface Config {\n    name: string\n  }\n}",
      "description": "Remove declare modifier from interface in ambient namespace"
    }
  ]
}