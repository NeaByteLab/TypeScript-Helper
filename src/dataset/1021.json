{
  "code": 1021,
  "message": "An index signature must have a type annotation.",
  "regexFind": "/\\[([a-zA-Z_$][a-zA-Z0-9_$]*\\s*:\\s*[^\\]]+)\\]/",
  "regexReplace": "[$1]: any",
  "suggestion": "Add a type annotation to the index signature. Index signatures must specify the type of their values.",
  "example": [
    {
      "base": "interface MyInterface {\n  [key: string]\n}",
      "fixed": "interface MyInterface {\n  [key: string]: any\n}",
      "description": "Add type annotation to index signature"
    },
    {
      "base": "interface DataInterface {\n  [prop: string]\n}",
      "fixed": "interface DataInterface {\n  [prop: string]: any\n}",
      "description": "Index signatures must have a type annotation for their values"
    }
  ]
}